import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    heading: `'CreativeVintageRegular', sans-serif`,
    body: `'Montserrat', serif`,
  },
})

export default theme
